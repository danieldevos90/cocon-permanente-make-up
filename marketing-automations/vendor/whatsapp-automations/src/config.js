/** Stub — vervangen door webpack alias wanneer ../whatsapp-automations bestaat. */
export const config = {
  dryRun: true,
  provider: 'stub',
  fallbackToEmail: false,
  meta: { apiVersion: 'v21.0', phoneNumberId: '', businessAccountId: '', accessToken: '', appSecret: '', webhookVerifyToken: '' },
  redis: { url: '', token: '' },
};
