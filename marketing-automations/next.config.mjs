/** @type {import('next').NextConfig} */
const nextConfig = {
  // Keep existing api/ folder as Vercel serverless - Next.js app/api/ for Next.js routes
  transpilePackages: [],
};

export default nextConfig;
